import { useCallback } from "react";

export function matify(text: string, enabled?: boolean) {
  void enabled;
  return text;
}

export function useMatText() {
  return useCallback((text: string) => text, []);
}
